# 백엔드 base URL (NestJS global prefix 포함)
VITE_API_BASE_URL=http://localhost:3000/api/v1

# JWT access token (브라우저 localStorage 키). apps/web 과 공유하거나
# 별도 관리자 계정용 토큰을 여기 넣는다.
VITE_ACCESS_TOKEN_KEY=kaster.access_token

# 도움말 내부 검토 모드. true 면 AUTO_DRAFT 도움말도 화면에 표시.
VITE_HELP_INTERNAL_REVIEW=false
