<UserControl x:Class="KAster.Desktop.App.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BrushBackground}">

    <!--
        지난 통화. 읽는 것은 넷이다 — 언제, 어느 번호와, 얼마나, 받았는지.
        줄을 누르면 그 번호가 발신 칸에 들어간다. 번호를 옮겨 적게 하지 않는다.
    -->
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="지난 통화" Style="{StaticResource TextCaption}" />

        <ItemsControl Grid.Row="1" Margin="0,8,0,0" ItemsSource="{Binding History}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Margin="0,0,0,4"
                            HorizontalContentAlignment="Stretch"
                            Style="{StaticResource ButtonSecondary}"
                            Command="{Binding DataContext.CallBackCommand,
                                              RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding PhoneNumber}"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding WasMissed}" Value="True">
                                                    <Setter Property="Foreground" Value="{StaticResource BrushDanger}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="{Binding When}" Style="{StaticResource TextCaption}" />
                            </StackPanel>

                            <TextBlock Grid.Column="1"
                                       Text="{Binding Duration}"
                                       VerticalAlignment="Center"
                                       Style="{StaticResource TextCaption}" />
                        </Grid>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Grid.Row="2" Margin="0,10,0,0" Content="닫기"
                Style="{StaticResource ButtonSecondary}"
                Command="{Binding CloseHistoryCommand}" />
    </Grid>
</UserControl>
